"""Data layer — external API clients (Birdeye, Helius).

Read-only in Phase 2: rank wallets and watch them live. No trading here.
All clients are config-driven and carry their own rate limiting so the free
API tiers aren't exceeded.
"""
